в этот раз достаточно объемное задание, а времени мало.
пройтись по работе второй раз (с учетом рекомендаций лекции) не успел.

вопрос про функции: в дз я старался по разумному делить код на функции, интересно получилось ли?

в дз 6 и дз 7 есть не очень понятный момент:
открываем файл на чтение, читаем по строкам
    with open(f_name, 'r') as file:
        for line in file:
            name, firm, earning, damage = line.split()
в таком варианте получется забить сразу несколько переменных, а вот когда мы (как в 6 сделано) содержимое забили в ответ фунции,
такой же фокус с переменными не прокатил. пришлось городить огороды....
