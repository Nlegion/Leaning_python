user_data = {
    'first_name',
    'last_name',
    'age',
    'height',
    'weight',
    'weight',
    'weight',
}

# list -> search O(n)
# hash table -> search O(1)

# print(type(user_data))
print(user_data)  # intersection
# print(dir(user_data))

# круги Эйлера

# a, b = b, a
